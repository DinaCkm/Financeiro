{
  "name": "painel-financeiro-gerencial-ckm",
  "version": "0.4.0",
  "private": true,
  "description": "MVP Sprint 4 - Painel Financeiro Gerencial CKM com suporte a PostgreSQL",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check storage.js && python3 -m py_compile scripts/parse_spreadsheet.py"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "multer": "^2.1.1",
    "openai": "^6.34.0",
    "pg": "^8.16.3"
  }
}
