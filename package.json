{
  "name": "painel-financeiro-gerencial-ckm",
  "version": "0.2.0",
  "private": true,
  "description": "MVP Sprint 2 - Painel Financeiro Gerencial CKM",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check storage.js && node --check scripts/migrate_json_to_pg.js && python3 -m py_compile scripts/parse_spreadsheet.py",
    "migrate:json-to-pg": "node scripts/migrate_json_to_pg.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "^8.16.3"
  }
}
